# DESIGN.md — 이 사이트의 시각 규칙

**시스템: Symplex** (Claude Design 프로젝트 `Vellum design system`, id `8fe76feb-30f7-4f85-a818-9c9cd42e97d0`).
Apple(17px 본문 · pill CTA · press scale) + Notion(웜 페이퍼 · 헤어라인 우선 elevation)
+ Figma(틴트 섹션 블록 · mono uppercase eyebrow)의 합성이며, **셋이 충돌하면 가장 조용한 쪽을 택한다**가 원칙이다.

시각적 변경 전에 이 파일을 먼저 읽는다. 규칙을 어기는 편이 낫다고 판단되면 어기기 전에 물어본다.

## 파일 경계

| 파일 | 역할 | 수정 규칙 |
|---|---|---|
| `static/css/symplex.css` | 토큰 + base + `.sx-*` 컴포넌트 | **직접 수정 금지.** 원본 시스템에서만 갱신 |
| `style.css` | 페이지 레이아웃 | 토큰 조합만. 리터럴 값 금지 |
| `static/js/bvh.js` | BVH 인스펙터 | — |

새 색·새 이징·새 반경·새 폰트를 **만들지 않는다.** 필요하면 시스템에 이미 있는 것을 찾는다.

## 절대 규칙 (검증 가능)

1. **그라디언트 0개.** 배경 그라디언트, 그라디언트 텍스트 전부 금지. 깊이는 표면 교체와 틴트 패널로 낸다.
2. **파랑은 `--accent-500` 하나뿐.** 다른 무엇도 파랗지 않다. 다크 표면에서는 `--accent-400`, 눌림은 `--accent-600`.
3. **틴트(`cream/lilac/sage/mist/night`)는 섹션에만.** 버튼·배지 채움·보더·텍스트 색으로 절대 쓰지 않는다.
4. **반전 밴드는 페이지당 하나.** 교대로 까는 다크 스택 금지.
5. **반경은 `0 / 4 / 8 / 12 / 18 / 28 / pill`만.** 중간값 금지. 인풋 8, 카드 18, 섹션 블록 28, 모든 CTA는 pill.
6. **카드는 흰 배경 + 1px `--hairline` + 그림자 없음.** 강조가 필요하면 elevation 대신 `--canvas-alt`로 가라앉힌다.
   `--shadow-media`는 이미지 전용이다.
7. **blur는 기능적일 때만** — frosted nav, 사진 위 반투명 칩, 모달 scrim. 장식용 글래스 금지.
8. **본문 17px.** 16px 아니다. Apple의 읽기 속도를 의도적으로 유지한다.
9. **웨이트는 300/400/500/600.** 700은 쓰지 않는다.
10. **mono(JetBrains Mono)는 uppercase eyebrow와 캡션 전용.** 본문에 쓰지 않는다.
11. **섹션 사이 96px**, 섹션 블록 내부 48px, 카드 내부 24px, 그리드 거터 24px.
12. **뷰포트당 primary 버튼은 하나.**
13. **이모지 없음.** 분류는 mono eyebrow와 배지가 진다. 아이콘 세트는 시스템에 없다.

## 모션

- 상태 변화 `--dur-fast` 120ms, 표면 변화 `--dur-base` 200ms. 곡선은 `--ease-standard` 하나.
- hover = 표면/색 한 단계 변화. press = `scale(0.97)`. focus = 2px accent 링, 표면에서 오프셋.
- `transition: all` 금지 — 속성을 명시한다.
- 상시 도는 애니메이션을 만들지 않는다. BVH 인스펙터는 드래그·리시드·리사이즈에서만 다시 그린다.
- `prefers-reduced-motion: reduce`면 전부 정지.
- hover 스타일은 `@media (hover: hover) and (pointer: fine)` 안에 두지 않아도 되지만,
  터치에서 오발동하면 즉시 가드를 건다.

## 카피

- 평서문, 2인칭, 문장형. 문장 부호는 완결된 문장일 때만 마침표.
- 버튼은 동사 우선 3단어 이내.
- 부제는 한두 문장. 세 문장은 길다.
- **숫자는 있는 그대로.** 지어낸 통계, 장식용 퍼센트 배지 금지.
  실측하지 않은 수치는 아예 슬롯을 만들지 말고, 불가피하면 `20XX` `0 min`처럼 플레이스홀더임이 보이게 둔다.
- 줄일 수 있는 문장은 틀린 문장이다.

## 이 사이트에만 적용되는 것

- **한국어**: `word-break: keep-all`. 48px 디스플레이 타입은 한글에서 줄바꿈 위치를 손으로 확인한다.
- **정보 구조**: 첫 화면에서 정체성 + 킬러 프로젝트가 잡혀야 한다(15초 룰). Selected work는 3개 고정.
  깊은 내용은 하위 페이지로 내린다.
- **링크는 루트 상대경로**(`/work/...`). 커스텀 도메인으로 옮겨도 안 깨진다.
- **SEO/AEO**: 새 페이지마다 canonical + OG + JSON-LD(`Person` / `BlogPosting`). 실제 발행일 전까지 `datePublished` 금지.
- **글은 마크다운 + frontmatter로 저장소에 남긴다.** 데이터 소유권이 GitHub Pages를 유지하는 이유다.
- **BVH 인스펙터는 장식이 아니라 도구다.** readout(`overlap`, `Σarea`)은 실제 계산값이어야 한다.
  이걸 애니메이션 배경으로 바꾸는 순간 규칙 위반이다.

## 미해결

- `/static/og.png` (1200×630) 없음. 지금 링크를 공유하면 카드가 깨진다.
- `/cv.pdf` 없음 — `/about/`의 CV 버튼은 현재 404다. 파일을 넣으면 바로 동작한다.
- 상세 페이지 미구현: `/work/ysim/`, `/work/collision-detection/`, `/work/glim/`, `/notes/<slug>/` 4건.
  목록에서 링크는 이미 걸려 있다.
- 작업물 프리뷰 이미지 없음. 가짜 스크린샷을 넣지 말고 `preview —` 빈 슬롯을 유지한다.
  `projects/*.md`에 `preview: /static/assets/x.png`를 넣으면 빌더가 `<img>`로 바꾼다.
- 글·작업 목록은 `posts/*.md` / `projects/*.md` frontmatter에서 생성된다.
  현재 양쪽 모두 0건이라 빈 상태 카드가 나온다. 이전 원본은 `archived-post/`에 있고 빌더는 읽지 않는다.
  읽기 시간은 글자 수 ÷ 700으로 **추정**한 값이다 — 실측이 아니며, 정확한 값이 필요하면 frontmatter로 덮어쓴다.
- 크로스포스팅 링크는 현재 텍스트다. 실제 URL을 걸려면 앵커 중첩 때문에 행 구조를 리팩터해야 한다.
- BVH 인스펙터는 현재 메인에 있다. `/work/ysim/`이 생기면 그쪽으로 옮기는 편이 자연스럽다.
- 900px 이하에서 nav 링크가 숨겨지고 대체 내비게이션이 없다. 브랜드 로고로만 홈에 간다.
  드로어를 시도했다가 되돌렸으므로, 다시 손대려면 다른 방식이 필요하다.
- 디자인 시안 후보군은 `design/`에 있으며 `.gitignore` 처리되어 있다.
