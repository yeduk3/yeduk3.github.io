.DS_Store
._.DS_Store
**/.DS_Store
**/._.DS_Store

# 디자인 시안 후보군 — 로컬 보관, 커밋하지 않음
design/

# 개인 보관용 — 커밋하지 않음
archived-post/

# 머신 로컬 권한 설정
.claude/settings.local.json

__pycache__/

# 원본 녹화. 인코딩한 mp4만 커밋한다 (빌더도 .mov는 복사하지 않는다)
articles-md/**/*.mov
projects-md/**/*.mov
